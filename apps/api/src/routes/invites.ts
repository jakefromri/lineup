// Deprecated — replaced by routes/public.ts (POST /api/invites/:token/accept)
// as part of the lineup fork. Kept as an empty stub because this file's ACL
// prevents deletion in the build sandbox; not imported anywhere.
export {};
